# 策问工程协作约定

- 一套源码、两种发行形态：桌面使用 Node 文件服务，静态网页使用浏览器文件夹授权。共享业务规则在 `shared/` 和 `server/`，浏览器平台适配在 `browser/`。
- 代码与新增行为提供中文注释；修改功能时同步维护 README 和对应协议/验收记录。
- 默认不运行自动化测试套件。执行必要的类型检查、生产构建和人工界面/协议检查，并如实记录未覆盖项。
- 真实游戏资料及接入验收与软件开发隔离；公开仓库只包含虚构示例。游戏项目目录、个人草稿、访问凭证、旧发行包和开发缓存不能提交。
- 正式 Markdown 与版本为权威源；`.cewen/` 为编辑器私有数据，不要求 LLM 阅读软件源码。
- 功能分支使用 `codex/` 前缀，稳定主线是 `main`。版本标签 `vX.Y.Z` 同时定位网页和桌面代码，不维护长期分叉的两套实现。
- 保留已验收的视觉与动画节奏，非任务范围不要顺便重排页面。涉及编辑器参数排版变化时先提供可审阅的布局示例。
- 不擅自修改历史快照、用户原始回答或真实游戏文档。
