# 0.9.0-rc.2 收尾验证记录

本记录区分源码实现、工程检查、协议仿真和用户环境验收。RC1 记录作为历史保留。

## 本地检查（2026-09-25）

| 范围 | 结果 |
|---|---|
| `npm run build:all` | 类型、桌面前端、网页前端、运行时构建通过；最终候选同时在 GitHub 验证 |
| `check-090.mjs scenario` | 共享地图隔离、旧哈希冲突、同请求重试、候选采用、历史、媒体哈希与完整导出通过 |
| `check-090.mjs migration` | 修复关闭后异步备份重建 watcher 问题；复跑退出码0，原项目字节与旧历史字节保留 |
| `check-090.mjs completion` | 唯一摘录/歧义/删除锚点、时间与按键节奏、子身份复制、语义生产依赖、选择/提示词修改、Quest增量和用户决定确认通过 |
| 教程编排夹具 | 未操作不能计为完成；稳定步骤恢复、明确跳过、部分完成结果分别验证 |
| 合成索引 | 500 文档、5,000 对象、4,500 引用；一次测量冷解析约705ms，10次缓存读取约0.01ms，进程heap约44MiB。不是跨硬件性能保证，也不是1GiB媒体I/O验证 |
| ComfyUI 仿真 | 提交确认、回环边界、并发重入、参考上传/节点绑定、负向映射、不确定提交、仅候选回导、安全取消通过；未运行实际生成模型 |
| 万类 | 包格式、身份/哈希/过期检测与跨项目拒绝通过；未连接真实编辑器 |

浏览器专项脚本 `scripts/check-090-browser.py` 对真实本地 HTTP 工作台操作，不拦截业务 API。远端运行日志与截图由本轮候选检查保存，结果必须以实际工作流为准，不从脚本存在推断通过。

## 必须由真实环境继续验收

Windows ZIP 原生运行、输入法/剪贴板/文件夹选择，浏览器文件夹权限往返，真实 ComfyUI 工作流及GPU，真实万类返回，长时间大型媒体库与设备音频同步。没有将协议仿真或合成数据标为上述实机验收。

## 关键发布保护

固定提交建标签，Pages 成功后只响应精确版本的 release-intent；候选发为 prerelease，不取代稳定最新版。已有标签不重指，已有 Release/附件不覆盖。ZIP 附带 SHA256 与源码 SOURCE.json。
