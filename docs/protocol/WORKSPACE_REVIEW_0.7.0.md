# 策问 0.7.0 目录与脑图验收记录

开发目录为 C:/Users/qbz19/Desktop/01Work/Designform_Dev。本轮仅使用基础示例和隔离的五层虚构项目，没有接入或改写真实游戏资料。

## 完成的行为

- 面板按钮按展开状态互斥，保留快捷键与个人显隐偏好。
- 总纲为固定根；分类和文档可以包含子项，公开层级与排序跟随正式版本。拖动前后位置用于排序，中部用于归入，路径节点用于返回上层。
- 分类与文档分栏只呈现当前层，两个路径导航支持快速返回；总纲始终可直接访问。公开分类及文档合计支持总纲以下五层。
- 写作工作台内提供文档引用目录，可浏览、搜索、点击插入或拖入。富文本按指针落点插入真实 Markdown 链接；源代码模式按当前文本光标插入；排版模式创建可移动的 Markdown 引用块。其他项目的拖入拒绝写入，不信任拖放载荷里的路径与标题。
- 新增设计脑图，点击卡片进入关系聚焦；星图、分层、脑图共用场景，分层和脑图可横竖切换。归属实线、关联虚线含义保持不变。脑图总览收束为层级树，聚焦后查看直接关系。
- 层级及关联是公开数据，坐标、方向和面板状态仍是个人编辑器数据。三维摆放不改变正文关系。

## 已执行验证

按工程约定没有运行自动化测试套件。类型检查、桌面前端/独立网页/本地运行时构建通过；Windows x64 便携包由同一源码构建。

1. 在隔离基础示例中，将“环境遭遇”拖入“地图结构”，保存的 DD 出现正确 parent，目录投影随版本更新。
2. 在正文引用目录中，将游戏总纲拖入专项正文，生成相对路径 ../gdd/GDD.md 的可点击链接；保存版本后磁盘 Markdown 保留该引用。
3. 五层样本依次进入“世界构想 → 地图结构 → 地图框架 → 区域通路 → 山谷捷径”；末级文档不增加缩进，路径可回到上层。修复了分类悬浮菜单遮住进入箭头的点击冲突。
4. 检查面板展开/收起按钮显隐、1440px 桌面布局与窄预览窗口目录；窄屏侧栏使用覆盖显示。
5. 点击设计脑图节点进入关系聚焦，检查返回和横竖切换；切换过程中控制台无新增错误。树根与分类颜色正确，深层内容可平移与缩放阅读。
6. 独立网页创建浏览器基础示例并进入脑图，目录与根文档展示正常。
7. 含知识锚点的示例进入正文编辑、拖入链接与保存，未再次出现 DOM 更新循环造成的冻结。

## 仍需用户复验

原生拖动手柄已实际操作；约 250ms 长按启动的逻辑已审查，但没有覆盖各触控板、触屏和系统缩放组合。未穷举超大型图谱、中文输入法组合、多窗口同时写入及所有旧项目异常数据。正式本机文件夹授权沿用既有实现，本轮网页人工检查使用浏览器虚构示例。源码模式拖入在当前光标插入，并非鼠标位置。

本记录说明开发侧检查范围，不代表用户人工验收已经通过。真实战术巫师接入仍在游戏项目的独立对话中开展。
